# Workspace formatting preferences
edition = "2021"
# Merge and sort imports by crate; reduces noise in diffs
imports_granularity = "Crate"
group_imports = "StdExternalCrate"
# Keep defaults for everything else to stay close to rustfmt stable

