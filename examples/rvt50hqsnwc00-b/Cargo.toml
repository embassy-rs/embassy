[package]
edition = "2024"
name = "embassy-rvt50hqsnwc00-b-examples"
version = "0.1.0"
license = "MIT OR Apache-2.0"
publish = false

description = "Examples for Riverdi RVT50HQSNWC00-B board with STM32U5A9NJH6Q"

[dependencies]
# RVT50HQSNWC00-B uses STM32U5A9NJH6Q with 4MB Flash, 2.5MB RAM
embassy-stm32 = { version = "0.6.0", path = "../../embassy-stm32", features = ["defmt", "unstable-pac", "stm32u5a9nj", "time-driver-any", "memory-x", "exti"] }
embassy-sync = { version = "0.8.0", path = "../../embassy-sync", features = ["defmt"] }
embassy-executor = { version = "0.10.0", path = "../../embassy-executor", features = ["platform-cortex-m", "executor-thread", "defmt"] }
embassy-time = { version = "0.5.1", path = "../../embassy-time", features = ["defmt", "defmt-timestamp-uptime", "tick-hz-32_768"] }
embassy-futures = { version = "0.1.2", path = "../../embassy-futures" }

defmt = "1.0.1"
defmt-rtt = "1.0.0"

cortex-m = { version = "0.7.6", features = ["inline-asm", "critical-section-single-core"] }
cortex-m-rt = "0.7.0"
embedded-hal = "0.2.6"
embedded-can = "0.4"
panic-probe = { version = "1.0.0", features = ["print-defmt"] }
static_cell = "2"

# oxivgl — safe Rust bindings over C LVGL v9.5 (Riverdi Cube-compatible sources).
# Capacitive touch (CTP_INT + I2C) is always enabled with this feature.
oxivgl = { version = "0.1.2", default-features = false, optional = true }
oxivgl-sys = { version = "0.1.2", default-features = false, optional = true }
embedded-alloc = { version = "0.6.0", features = ["llff"], optional = true }

## Use secure registers when TrustZone is enabled
[features]
default = []
oxivgl = ["dep:oxivgl", "dep:oxivgl-sys", "dep:embedded-alloc"]
trustzone-secure = ["embassy-stm32/trustzone-secure"]

[[bin]]
name = "oxivgl_widget_demo"
required-features = ["oxivgl"]

[profile.release]
debug = 2

# Align oxivgl's embassy crates with this example's path dependencies.
[patch.crates-io]
embassy-time = { path = "../../embassy-time" }
embassy-sync = { path = "../../embassy-sync" }
oxivgl = { path = "vendor/oxivgl" }
oxivgl-sys = { path = "vendor/oxivgl-sys" }

[package.metadata.embassy]
build = [
  { target = "thumbv8m.main-none-eabihf", artifact-dir = "out/examples/rvt50hqsnwc00-b" }
]
